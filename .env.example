# 把这个文件复制成 .env.local 并填上你的 OpenAI API key
# https://platform.openai.com/api-keys

OPENAI_API_KEY=sk-...

# 数据目录，默认 ./data；可以改成绝对路径（比如放到 iCloud/Dropbox 同步文件夹）
# DATA_DIR=/Users/you/Documents/dailynote-data
